<h1>Show Reservation</h1>
<p><%= @reservation.date_time %></p>
<p><%= @reservation.party_size %></p>
<p><%= @reservation.user_id %></p>
<p><%= @reservation.restaurant_id %></p>

  <%= link_to("Delete", reservation_path(@reservation), method: :delete, data: {confirm: "Are you sure you want to delete this reservation?"})  %>
<hr>
